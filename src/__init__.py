"""MRNet classification project — source package.

This package is imported by the notebooks in ``codes/notebooks``.
Each module is owned by a team member (see ``Project Pipeline.md``).

Nothing here implements model/training logic on purpose — every module
ships as a scaffold of signatures + docstrings for its owner to fill in.
"""
